# Tobira デモ環境ガイド

本ドキュメントでは、Tobiraの各種機能（複雑なグループ権限、アプリ・サービスの階層管理など）を検証するための**デモ環境の構築手順**と、**デモ用アカウントの一覧**、および**デモアプリの動作確認手順**について説明します。

---

## 🛠️ デモ環境の一発構築

既存のローカルデータベースをリセットし、豊富なデモデータを流し込んだ初期状態の環境を一発で構築できます。

```bash
# パッケージのインストール（初回のみ）
npm install

# ローカルデモ環境の構築（DBスキーマ生成＋データ投入）
npm run demo:setup

# ローカルサーバーの起動
npm run dev
# -> http://localhost:8787/login
```

> **注意:** `npm run demo:setup` を実行すると、ローカルのD1データベース（`.wrangler`ディレクトリ内）が完全にリセットされ、初期状態に戻ります。

### リモート(本番)環境へのデモデータ投入

リモートのCloudflare D1にスキーマとデモデータを適用したい場合は、以下のコマンドを実行します。

```bash
npm run demo:setup -- --remote
```

> **注意:** リモートに対する実行では、ローカルのようにデータベース自体のリセット（ファイル削除）は行われません。そのため、既存のテーブルがある場合にデータが重複・衝突する可能性があります。

#### 既存データを消去してから再構築する場合（イニシャライズクリア）

リモートのD1にあるすべてのテーブルを一度 `DROP` し、真っさらの状態から構築し直す場合は、`--clear` オプションを付与してください。

```bash
npm run demo:setup -- --remote --clear
```

#### 単独でのデータベース初期化

デモの投入は行わず、単にすべてのテーブルを削除して初期化したい場合は、専用のクリアコマンドを使用できます。実行時に確認プロンプトが表示されます。

```bash
# ローカルDBのクリア
npm run db:clear

# リモートDBのクリア
npm run db:clear -- --remote
```

---

## 🔑 デモ用アカウント一覧

デモ環境には、異なる権限を持つ複数のアカウントが初期設定されています。
**すべてのパスワードは `admin1234` です。**

### 1. グローバル管理者
システム全体の管理（ユーザー、グループ、アプリなどの全て）が可能です。
- **Email:** `admin@example.com`

### 2. グループ管理者（3段ヒエラルキーの中間：開発部門）
本社（Root） > 開発部門（Middle） > 第一開発チーム（Leaf）の3段ヒエラルキーのうち、**「開発部門」**の管理者アカウントです。権限別に4種類のアカウントを用意しています。

| アカウント名 | Email | 決済権限 | 開発権限 | 備考 |
|---|---|:---:|:---:|---|
| **全権限持ち** | `g_admin_all@example.com` | ✅ | ✅ | すべてのグループ操作が可能 |
| **決済のみ** | `g_admin_billing@example.com` | ✅ | ❌ | 利用枠などの予算操作のみ可能 |
| **開発のみ** | `g_admin_dev@example.com` | ❌ | ✅ | サービス・アプリの登録・管理が可能 |
| **素の管理者** | `g_admin_basic@example.com` | ❌ | ❌ | 権限なしの素のグループ管理者 |

*※「開発部門」のG管理者（開発権限あり）が、「社内ポータルサービス」とそれに紐づく「ユーザー登録アプリ」「社内Wikiアプリ」を作成したという想定でデータが投入されています。*

### 3. 一般メンバー（末端チーム：第一開発チーム）
- **Email:** `member_user@example.com`
- **備考:** 「開発部門」の配下である「第一開発チーム」に所属する一般ユーザーです。「ユーザー登録アプリ」と「社内Wikiアプリ」の利用権限が付与されています。

---

## 🚀 デモアプリ（OIDC クライアント）の動作確認

TobiraのAuthorization Code フロー（PKCE対応）を検証するための、完全動作するOIDCクライアント例が `examples/cf-demo` に用意されています。

### 実行手順

1. **Tobira (IdP) 側の準備**
   - 上記の `npm run demo:setup` と `npm run dev` でローカルサーバーを起動しておきます。
   - デモデータとして、`app-user-reg` (http://localhost:3000) などが登録済みです。

2. **デモアプリの起動**
   別のターミナルを開き、以下のコマンドを実行します。

   ```bash
   cd examples/cf-demo
   npm install
   
   # デモアプリの起動（ポート3000）
   npm run dev
   ```

3. **ログインのテスト**
   - ブラウザで `http://localhost:3000` にアクセスします。
   - **Login** ボタンを押すと、Tobiraのログイン画面 (`http://localhost:8787`) にリダイレクトされます。
   - 上記のデモ用アカウント（例: `member_user@example.com` / `admin1234`）でログインします。
   - 認証に成功すると、デモアプリに戻り、IDトークン（JWT）の検証結果とユーザー情報が表示されます。
